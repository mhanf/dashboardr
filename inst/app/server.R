
# serveur
server <- function(input, output, session) {

#
#   data <- data.frame(x = rnorm(10, 5, 3), y = rnorm(10, 50, 12), z = rnorm(10, 50, 5))
#
#
#   output$plot <- echarts4r::renderEcharts4r({
#     data |>
#       e_charts(x) |>
#       e_scatter(y) |>
#       e_scatter(z) |>
#       e_brush(throttleDelay = 1000)
#   })
#
#
#   r <- reactiveValues()
#   r$mtcars <- mtcars
#   r$bgcolor <- "danger"
#
#   observeEvent(input$click, {
#     r$mtcars <- mtcars[mtcars$vs == 1, ]
#     r$bgcolor <- "success"
#   })

  # lapply(element,function(i){
  #     mod_infobox_server(id = i,
  #                    df,
  #                    r = r,
  #                    pattern = "^%r%"
  #     )
  #   })

}
